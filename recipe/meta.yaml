{% set name = "heavyai" %}
{% set version = "1.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/heavyai-{{ version }}.tar.gz
  sha256: e1e27fe294bf1ab4df933c98884d16527777650f9b6c996618d2c90ad6b45fa1

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.7
    - flit-core
  run:
    - importlib_metadata
    - numpy
    - pandas
    - pyarrow >=3.0.0
    - pyheavydb
    - python >=3.7
    - shapely

test:
  imports:
    - heavyai

about:
  home: https://github.com/heavyai/heavyai
  summary: Data science toolkit support for HeavyDB
  dev_url: https://github.com/heavyai/heavyai
  doc_url: https://heavyai.readthedocs.io/
  license: Apache-2.0
  license_file: LICENSE.md
  description: |
    This package enables using common Python data science toolkits with
    HeavyDB. It brings dataframe support on CPU and GPU as well as
    support for arrow.

extra:
  recipe-maintainers:
    - guilhermeleobas
    - jack-mapd
    - tupui
    - pearu
